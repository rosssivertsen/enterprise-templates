[pytest]
testpaths = tests
markers =
    unit: Unit tests (fast, no external dependencies)
    e2e: End-to-end integration tests (require running services)
    slow: Slow tests (skip in quick iterations)
addopts = -v --tb=short --strict-markers
